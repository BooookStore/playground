package io.booookstore.handler

import io.ktor.server.response.*
import io.ktor.server.routing.*

suspend fun createPotentialCombination(context: RoutingContext) {
    // TODO
    context.call.respondText("TODO")
}